#define NULL 0 ///< Unassociated pointer default value
#define BIT_PER_BYTE 8 ///< Number of bit per byte

#define INVALIDATION_BYTE 0xEE ///< Debug byte memory clear pattern (used to catch bad memory allocation)